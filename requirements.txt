# Dependências do tooling de imagem — tools/photo_to_outline.py
# Instalar SOMENTE no venv isolado tools/.venv/ (a suíte OpenSCAD segue stdlib-pura):
#   python -m venv tools/.venv
#   tools/.venv/Scripts/python -m pip install -r tools/requirements.txt
#
# Verificado em Python 3.14 (Windows): há wheel abi3 do opencv-python que cobre 3.14.
numpy>=2.5,<3
opencv-python>=4.13,<5
